import { add } from "./calculator";

it("add correctly", () => {
  expect(add(3, 5)).toBe(8);
});

it("subtract correctly", () => {
  expect(add(3, 5)).toBe(8);
});

it("multiple correctly", () => {
  expect(add(3, 5)).toBe(8);
});

it("divide correctly", () => {
  expect(add(3, 5)).toBe(8);
});
